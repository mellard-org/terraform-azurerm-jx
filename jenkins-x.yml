buildPack: none
dockerRegistryOwner: jenkinsxio
pipelineConfig:
  pipelines:
    pullRequest:
      pipeline:
        stages:
          - agent:
              image: gcr.io/jenkinsxio/builder-go-nodejs
            name: release
            environment:
              - name: GITHUB_ACCESS_TOKEN
                valueFrom:
                  secretKeyRef:
                    name: terraform-azurerm-git-user-pass
                    key: password
            steps:
              - name: echo
                command: echo release pipeline not yet implemented
